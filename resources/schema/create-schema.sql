CREATE TABLE subscribers (
   jid VARCHAR(300),
   email VARCHAR(300),
   PRIMARY KEY (jid)
);
CREATE INDEX subscribers_jid_index ON subscribers(jid);

CREATE TABLE subscribers (
   jid VARCHAR(300),
   email VARCHAR(300),
   PRIMARY KEY (jid)
);
CREATE INDEX subscribers_jid_index ON subscribers(jid);
